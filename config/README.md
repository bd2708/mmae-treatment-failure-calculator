# Configuration

## `model_config.json`

Model hyperparameters and training configuration for the Gradient Boosting Classifier.

These parameters can be modified to retrain the model on new data using `src/export_model_to_onnx.py`.
